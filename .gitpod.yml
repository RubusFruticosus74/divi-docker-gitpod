tasks:
  - init: |
      docker-compose pull
  - command: |
      docker-compose up


ports:
  - port: 5000
    onOpen: ignore
  - port: 3000
    onOpen: open-browser

vscode:
  extensions:
    - ms-azuretools.vscode-docker
